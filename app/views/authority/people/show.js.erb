$("div.creator").each(function(){
   $(this).find("select.creator-id").append($(document.createElement("option")).
           attr("value","<%=  @authority_object.id %>").text("<%=  @authority_object.display_value %>"));
});
$("div.hidden-creator").each(function(){
    $(this).find("select.creator-id").append($(document.createElement("option")).
            attr("value","<%=  @authority_object.id %>").text("<%=  @authority_object.display_value %>"));
});
$("div.creator").find("select.creator-id").last().val("<%= @authority_object.id %>");
$("#new_agent_modal").modal('hide');
$("#new_agent_modal").find('form input[type!="submit"]').val('');