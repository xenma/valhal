<h1>Listing <%= @klazz.to_s %> objects</h1>

<table class="table">
  <thead>
  <tr>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>
  <% @authority_bases.each do |person| %>
      <tr>
        <td><%= link_to person.display_value, person %></td>
        <td><%= link_to 'Edit', edit_authority_person_path(person) %></td>
        <td><%= link_to 'Destroy', authority_person_path(person), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
  <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Base', new_authority_person_path %>